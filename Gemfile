source "https://rubygems.org"

# Use the GitHub Pages gem — locks all dependency versions to what
# GitHub Pages actually supports, preventing local/remote mismatches.
gem "github-pages", group: :jekyll_plugins

group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
